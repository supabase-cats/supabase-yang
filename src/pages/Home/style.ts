import { css } from '@emotion/react';

const styles = {
  main: css`
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
  `,
};

export default styles;
